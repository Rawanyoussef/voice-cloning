# 🎙️ مشروع استنساخ الصوت - Voice Cloning Project

مشروع استنساخ الصوت باستخدام Coqui TTS وXTTS v2 لتوليد صوت عربي بناءً على عينة صوتية مرجعية.

## 📋 المتطلبات

- Docker & Docker Compose
- ملف صوتي بصيغة WAV (كعينة مرجعية)

## 🏗️ هيكل المشروع

```
project/
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── run_tts.py
├── input/
│   └── audio.wav          # ضع الملف الصوتي المرجعي هنا
└── output/
    └── generated_voice.wav  # سيتم إنشاء الصوت المولد هنا
```

## 🚀 خطوات التشغيل

### 1. تحضير المشروع

```bash
# إنشاء هيكل المجلدات
mkdir -p input output

# وضع الملف الصوتي المرجعي
# ضع ملف audio.wav في مجلد input/
```

### 2. بناء وتشغيل Docker

```bash
# بناء الـ Image
docker-compose build

# تشغيل المشروع
docker-compose up
```

### 3. النتائج

- الملف المولد سيكون في: `output/generated_voice.wav`
- أول مرة تشغيل ستستغرق وقت أطول لتحميل النموذج (حوالي 1-2 جيجا)
- المرات القادمة ستكون أسرع لأن النموذج محفوظ في cache

## 🔧 تخصيص النص

لتغيير النص المراد تحويله لصوت، افتح ملف `run_tts.py` وعدل المتغير `text`:

```python
text = """
النص العربي الذي تريد تحويله لصوت
"""
```

## 🛠️ أوامر مفيدة

```bash
# عرض اللوجات
docker-compose logs -f

# إيقاف المشروع
docker-compose down

# إعادة البناء والتشغيل
docker-compose up --build

# حذف الـ cache (إذا أردت تحميل النموذج من جديد)
docker-compose down -v
```

## ⚙️ الإصدارات المستخدمة

- Python: 3.10
- TTS: 0.22.0
- PyTorch: 2.1.0
- Transformers: 4.33.0
- Model: XTTS v2 (Multilingual)

## 📝 ملاحظات مهمة

1. **صيغة الملف الصوتي**: يجب أن يكون WAV (يمكن تحويل MP3 باستخدام FFmpeg)
2. **جودة الصوت المرجعي**: كلما كان الصوت المرجعي أوضح، كلما كانت النتيجة أفضل
3. **طول العينة**: يفضل أن تكون العينة الصوتية من 6-10 ثواني
4. **اللغة**: المشروع معد حالياً للغة العربية (ar)

## 🐛 حل المشاكل

### المشكلة: لم يتم العثور على ملف WAV
**الحل**: تأكد من:
- وجود الملف في `input/audio.wav`
- صيغة الملف WAV وليس MP3
- Docker لديه صلاحية الوصول للمجلد

### المشكلة: خطأ في تحميل النموذج
**الحل**: 
- تأكد من اتصالك بالإنترنت
- حاول حذف الـ cache: `docker-compose down -v`
- أعد البناء: `docker-compose up --build`

### المشكلة: الصوت المولد غير واضح
**الحل**:
- استخدم ملف صوتي مرجعي بجودة أعلى
- تأكد من وضوح الصوت المرجعي وخلوه من الضوضاء
- جرب ملف صوتي مرجعي أطول (6-10 ثواني)

## 📄 الترخيص

المشروع يستخدم Coqui TTS بترخيص CPML (غير تجاري). للاستخدام التجاري، راجع ترخيص Coqui.